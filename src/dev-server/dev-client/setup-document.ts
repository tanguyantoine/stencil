

export function setupDocument(doc: Document) {
  removeDevServerScripts(doc);

}


function removeDevServerScripts(_doc: Document) {
  // const devScripts = doc.querySelectorAll('script[data-dev-server-script]');

  // for (let i = 0; i < devScripts.length; i++) {
  //   devScripts[i].parentNode.removeChild(devScripts[i]);
  // }
}
